services:
  meet-redirect:
    build: .
    ports:
      - "8080:8080"
    environment:
      - MEET_URL
      - PORT
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: always

